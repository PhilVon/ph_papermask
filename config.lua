Config = {
    -- Is a Pen Required to alter Paper bag
    penNeeded = true,
    -- Name of the Item that is used to alter Paper bag
    penItem = 'markerpen',

    -- Prefix of your Image files in ox_inventory
    imagePrefix = 'papermask',

    ---------------------------------------------------------------------------------------
    -- Custom Theme -- r = red(0-255), b = blue(0-255), g = green(0-255), a = alpha(0-1)
    ---------------------------------------------------------------------------------------
    -- color for the left side of the popup, where mask images are displayed
    colorImgContainer = { r = 52, g = 73, b = 94, a = .8 },
    -- color for the right side of the popup, where the buttons are displayed
    colorBtnContainer = { r = 44, g = 62, b = 80, a = 1 },
    -- color for the buttons
    colorBtn = { r = 41, g = 128, b = 185, a = 1 },
    -- color for when a button is hovered over
    colorbtnHover = { r = 52, g = 152, b = 219, a = 1},
    -- color for the text on the buttons
    colorBtnText = { r = 255, g = 255, b = 255, a = 1},

    -- color of the scrollbar track
    colorScrollTrack = { r = 26, g = 37, b = 47, a = 1 },
    -- color of the scrollbar thumb (the thing you can drag)
    colorScrollThumb = { r = 37, g = 52, b = 68, a = 1 },
    -- color of the scrollbar thumb when hovered over
    colorScrollThumbHover = { r = 50, g = 69, b = 88, a = 1 },
}